# Pulls remote git changes. Use `/pull resolve` or `/pull -r` to attempt resolving any potential conflicts.
Pull git changes from the remote server
If $ARGUMENTS has `-r` or case insensitive `resolve` you should attempt to resolve git conflicts that can be reliably resolved. Do not resolve conflicts when you are unsure what the correct resolution is.