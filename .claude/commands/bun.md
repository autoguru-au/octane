# Installs dependencies and runs relay and format commands
Run this command from the monorepo root folder
`bun install && bun run format`

If there were errors with the bun install command, then run `bun run manypkg fix` commands and `bun install && bun run format` command again
