import { Button } from '@autoguru/overdrive';

const Componet = () => {
	return (
		<>
			<button onClick={() => {}} />
			<Button onClick={() => {}} />
			<a href="" />
		</>
	);
};

const AnotherComponent = () => {
	return (
		<>
			<button onClick={() => {}} />
			<Button onClick={() => {}} />
			<a href="" />
		</>
	);
};
